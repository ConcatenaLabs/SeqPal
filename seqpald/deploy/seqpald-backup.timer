[Unit]
Description=Back up the seqpald database every six hours

[Timer]
OnCalendar=*-*-* 00,06,12,18:20:00 UTC
# A box that was off at the scheduled minute still gets its backup.
Persistent=true
AccuracySec=1min

[Install]
WantedBy=timers.target
